import React from 'react'
import style from '../styles/Loading.module.css'

const Loading = () => {
  return <div className={`flexible ${style.loading}`}>
    <span></span>
  </div>
}

export default Loading
